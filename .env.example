# Copy this file to .env and fill in real values.
# start.sh automatically loads this file if it exists in the project root.

# Required for the AI chat feature
OPENROUTER_API_KEY=sk-or-...

# Optional — defaults to /app/docs inside the container
# DOCS_PATH=/app/docs
